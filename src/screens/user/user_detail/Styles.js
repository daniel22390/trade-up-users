import { StyleSheet } from 'react-native'


const styles = StyleSheet.create({
    container: {
        padding: 15
    },
    value: {
        fontSize: 18
    },
    label: {
        color: '#666',
        fontWeight: 'bold'
    }
})

export default styles